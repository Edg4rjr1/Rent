source 'https://rubygems.org'


gem 'bootsnap', '>= 1.4.4', require: false

gem 'importmap-rails'
gem 'rails', '~> 8.0'




# Gem principal do Rails
gem 'devise'  #Autenticação
gem 'bootstrap', '~> 5.0'
gem 'prawn'
gem 'csv'  # Para manipulação de CSV
gem 'kaminari'
gem 'kaminari-bootstrap'
gem 'prawn-table'


# Banco de dados (MySQL, por exemplo)
gem 'mysql2', '>= 0.5.3'

# Para upload de arquivos 
gem 'carrierwave'

# Frontend e estilos
gem 'sassc-rails' # Caso precise usar Sass com o Bootstrap

# Outros
gem 'jbuilder', '~> 2.7'  # Para criar APIs (opcional)
gem 'puma', '~> 6.0'

group :development, :test do
  gem 'byebug', platforms: %i[mri mingw x64_mingw]
end

group :development do
  gem 'web-console', '>= 4.1.0'
  gem 'listen', '~> 3.3'
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.1.0'
end